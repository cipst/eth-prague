import { BridgeShares } from "@/components/bridge/BridgeShares";

export const Bridge = () => {
    return (
        <div className="flex flex-wrap items-start justify-center gap-5">
            <BridgeShares />
        </div>
    );
};
